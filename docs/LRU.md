# LRU（最近最少使用）缓存算法

LRU（Least Recently Used）缓存是一种常用的缓存淘汰策略。它会移除最近最久未被访问的数据。

## 原理简介
- 每次访问数据时，将其标记为最新使用。
- 当缓存满时，淘汰最久未被访问的数据。

## 主要接口
- `put(key, value)`: 插入或更新缓存项。
- `get(key, value)`: 查询缓存项，命中返回 true 并更新使用顺序。

## 适用场景
- 数据访问有明显的时间局部性。

## 相关实现
- 详见 `EasyLruCache.h`。

