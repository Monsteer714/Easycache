# LFU-Aging 算法

LFU-Aging 是 LFU 的改进版，通过定期衰减计数，避免老数据长期占用缓存。

## 原理简介
- 定期对所有计数进行衰减。
- 使新热点数据有机会进入缓存。

## 主要接口
- `put(key, value)`: 插入或更新缓存项。
- `get(key, value)`: 查询缓存项。

## 适用场景
- 热点数据随时间变化。

## 相关实现
- 详见 `EasyLfuCache.h`。

