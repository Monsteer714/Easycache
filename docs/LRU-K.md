# LRU-K 缓存算法

LRU-K 算法是 LRU 的扩展，只有被访问 K 次的数据才会进入缓存。

## 原理简介
- 维护历史访问记录。
- 只有访问次数达到 K 的数据才被缓存。

## 主要接口
- `put(key, value)`: 插入或更新缓存项。
- `get(key, value)`: 查询缓存项。

## 适用场景
- 过滤掉偶发访问的数据，适合有重复访问的数据场景。

## 相关实现
- 详见 `EasyLruCache.h`（EasyKLruCache）。

